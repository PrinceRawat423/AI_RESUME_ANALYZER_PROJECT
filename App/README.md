# AI Resume Analyzer - Streamlit version

Run from this folder:

```bash
pip install -r requirements.txt
streamlit run App.py
```

If MySQL is not available, the app still runs and skips database saves.
